rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Pharmacy-scoped data: only authenticated users can access their pharmacy
    match /pharmacies/{pharmacyId}/{collection}/{document=**} {
      allow read, write: if request.auth != null;
    }

    // Deny everything else
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
